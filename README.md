# Unpublished

This explorer is off the public web. Served tree emptied 2026-09-02.

GitHub Pages is disabled in this repository (no workflow, `has_pages` is false). The live URL `https://mikeornstein.com/mpfl-loop-explorer/` is not hosted here. It is a folder on the user site:

- repo: `mikeornstein/mikeornstein.github.io`
- path: `static/mpfl-loop-explorer/`

That folder was deleted on the user-site `main` branch so the custom-domain path 404s after the Hugo Pages deploy.
